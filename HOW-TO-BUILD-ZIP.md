# Générer l'archive du plugin

Les fichiers binaires ne sont pas versionnés ici.

Depuis la racine du dépôt :

```bash
zip -r bertin-realisations.zip bertin-realisations
```

Ensuite, téléversez `bertin-realisations.zip` dans WordPress:
- Extensions > Ajouter > Téléverser une extension.
